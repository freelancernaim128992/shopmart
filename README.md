# shopmart
Live link: https://magenta-croquembouche-5ab4db.netlify.app/

1. #Difference Between null and Undefined
Null is a javascript variable where programmer intentionally defined it in a variable. It means unknown value or not assigned value. On the other hand, undefined is a not assigned variable. It show when a user or programmer not define any value of a variable.
2. #What is the use of the map() function in JavaScript? How is it different from forEach()?

map() is uses for return a new array from existing array and forEach() is not return anything but it can do something with each element inside the array

3.#What is the difference between == and ===?
== use to equal for type not value but === use for equal type and value.

4. #What is the significance of async/await in fetching API data?
The significance of asynce/ await is cleaner code and error handling. It make easier in multiple async operations.

5. #Explain the concept of Scope in JavaScript (Global, Function, Block).
golbal scope means you can access the variable form anywhere in the code and Function scop means you can access the variable just inside the function from anywhere but can't use outside from the functions. Also, block scope means you can access variable in {}.